---
# Copy this whole _template/ FOLDER to content/misc/rides/<slug>/ to add a ride
# to the Misc "Biking" block. Keep the file named index.md; drop the route image
# in the folder next to it. (The leading underscore keeps _template out of the
# manifest, so this sample never renders.)
title: Ride name          # shown on the card (and used as the manifest key)
distance: 42 km           # shown under the name
link: "#"                 # opens in a NEW TAB when the card is clicked (not shown as text)
image: ./route.png        # route image, co-located in this folder (png / jpg / svg)
---
